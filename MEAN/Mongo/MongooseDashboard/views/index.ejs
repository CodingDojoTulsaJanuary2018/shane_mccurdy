<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Mongoose Dashboard</title>
    <style media="screen">
      .wrapper{
        width: 600px;
        margin: 50px auto;
        font-family: verdana;
        font-size: 1.3em;
        text-align: center;
      }
      table{
        border: thick solid black;
        margin: 30px auto;
        width: 100%;
        border-collapse: collapse;
      }
      th{
        color: white;
        background: black;
        padding: 10px 20px;
      }
      td{
        padding: 5px 5px;
      }
      .button{
        color: black;
        text-decoration: none;
        background: white;
        padding: 5px 20px;
        font-family: verdana;
        font-size: 1em;
        border: black solid 4px;;
        box-shadow: 5px 5px 0 black;
      }
      .button:hover{
        background-color: #eee;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
         <h3><%= errors[x].message %></h3>
        <% } %>
      <% } else { %>

      <h1>Mongoose List</h1>

      <table>
        <tr>
          <th>Name</th><th>Age</th><th>ID</th>
        </tr>
        <% if (pack.length != 0){ %>
          <% for (let x in pack){ %>
          <tr>
            <td><%= pack[x].name %></td><td><%= pack[x].age %></td><td><a href="/mongooses/<%= pack[x]._id %>"><%= pack[x]._id %></a></td>
          </tr>
        <% } %>
      <% } %>
      </table>
      <a class="button" href="/mongooses/new">Create A New One</a>
    <% } %>
    </div>
  </body>
</html>
